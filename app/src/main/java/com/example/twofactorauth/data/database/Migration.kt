package com.example.twofactorauth.data.database

import androidx.room.migration.Migration

object Migrations {
    // Add a tested Migration here whenever AppDatabase.version is increased.
    fun getAllMigrations(): Array<Migration> = emptyArray()
}
